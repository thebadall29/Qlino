const whatsappConfig = {
    accessToken: process.env.WHATSAPP_ACCESS_TOKEN,
    phoneNumberId: process.env.WHATSAPP_PHONE_NUMBER_ID,
    // Add any other WhatsApp-related configuration here
};

module.exports = whatsappConfig;
